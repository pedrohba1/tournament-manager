import { Player } from '../types/Player';
import { Tournament } from '../types/Tournament';

export default function getRemoteStandings(tournament: Tournament): Player[] {
  const { players } = tournament;

  //order these players by position
}
